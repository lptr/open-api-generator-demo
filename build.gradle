plugins {
    id 'java-library'
    id 'application'
}

dependencies {
    compile project(path: ":open-api", configuration: "runtimeElements")
}

application {
    mainClassName = "org.example.vava.App"
}
